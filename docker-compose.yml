version: '3.7'
services:
  #server:
    # build: ./server
    # #image: 'node:20'
    # container_name: backend
    # hostname: backend
    # working_dir: /server
    # command: npm run dev
    # volumes:
    #   - ./server:/server
    # ports:
    #   - "8080:8080"
    # environment:
    #   NODE_ENV: dev      
  mongo:
    #build: ./mongo
    image: 'mongo'
    restart: 'always'
    environment:
      - 'MONGO_INITDB_ROOT_USERNAME=root'
      - 'MONGO_INITDB_ROOT_PASSWORD=root'
    volumes:
      - './mongo/data:/data/db'
    ports:
      - "27017:27017"
volumes:
  mongo_data: